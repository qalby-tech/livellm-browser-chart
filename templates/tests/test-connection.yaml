apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "livellm-browser.fullname" . }}-test-connection"
  labels:
    {{- include "livellm-browser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "livellm-browser.fullname" . }}:{{ .Values.service.api.port }}']
  restartPolicy: Never
